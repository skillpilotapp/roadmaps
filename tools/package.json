{
  "name": "@skillpilot/roadmaps-tools",
  "version": "1.0.0",
  "description": "Validation and asset generation for the roadmaps dataset. The dataset itself has no dependencies.",
  "type": "module",
  "private": true,
  "scripts": {
    "validate": "node validate.mjs",
    "check-links": "node check-links.mjs",
    "render-assets": "node render-assets.mjs",
    "render-markdown": "node render-markdown.mjs",
    "check-markdown": "node render-markdown.mjs --check"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
